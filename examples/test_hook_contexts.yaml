config:
  name: "Hook Contexts Demo"
  base_url: ${ENV(BASE_URL)}
  setup_hooks:
    - ${setup_hook_sign_request($request)}
  teardown_hooks:
    - ${teardown_hook_capture_request_id($response)}
steps:
  - name: get root with hooks
    request:
      method: GET
      url: /
    validate:
      - eq: [status_code, 200]


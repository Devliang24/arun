config:
  name: "参数化：矩阵"
  base_url: ${ENV(BASE_URL)}
parameters:
  # 2 x 2 组合，共 4 组
  a: [1, 2]
  b: ["x", "y"]
steps:
  - name: 回显参数
    request:
      method: GET
      url: /?a=$a&b=$b
    validate:
      - eq: [status_code, 200]

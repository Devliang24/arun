config:
  name: "Example: Suite Hooks"
  base_url: ${ENV(BASE_URL)}
  setup_hooks:
    - ${setup_hook_sign_request($request)}
  teardown_hooks:
    - ${teardown_hook_capture_request_id($response)}
cases:
  - config:
      name: Case In Suite
      setup_hooks:
        - ${setup_hook_sign_request($request)}
    steps:
      - name: get root
        request:
          method: GET
          url: /
        validate:
          - eq: [status_code, 200]

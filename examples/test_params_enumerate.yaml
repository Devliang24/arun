config:
  name: "Parameters Enumerate"
  base_url: ${ENV(BASE_URL)}
parameters:
  - { a: 1, b: "x" }
  - { a: 2, b: "y" }
steps:
  - name: enumerated params
    request:
      method: GET
      url: /?a=$a&b=$b
    validate:
      - eq: [status_code, 200]


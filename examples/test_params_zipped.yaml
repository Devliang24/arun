config:
  name: "参数化：压缩"
  base_url: ${ENV(BASE_URL)}
parameters:
  - a-b:
      - [1, "x"]
      - [2, "y"]
steps:
  - name: 压缩参数
    request:
      method: GET
      url: /?a=$a&b=$b
    validate:
      - eq: [status_code, 200]

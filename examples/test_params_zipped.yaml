config:
  name: "Parameters Zipped"
  base_url: ${ENV(BASE_URL)}
parameters:
  - a-b:
      - [1, "x"]
      - [2, "y"]
steps:
  - name: zipped params
    request:
      method: GET
      url: /?a=$a&b=$b
    validate:
      - eq: [status_code, 200]

